name: flutter_background_service_methodchannel
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  device_info_plus: ^9.1.0
  flutter:
    sdk: flutter
  flutter_background_service: ^5.0.3
  flutter_local_notifications: ^16.1.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
